<%= simple_form_for [@bike, @booking, @review] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :comment %>
    <%= f.input :rating , collection: 1..5%>
  </div>

  <%= f.button :submit %>
  </div>
<% end %>
